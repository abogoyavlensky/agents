# swappiness=10 keeps swap as an overcommit safety net rather than a routine
# destination. With no swap at all, a bursty agent workload turns an
# overcommit into an instant OOM kill instead of a slowdown.
vm.swappiness=10
vm.vfs_cache_pressure=50
