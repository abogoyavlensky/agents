# cp inventory.yml.example inventory.yml and fill in the real address.
# inventory.yml is gitignored - it holds host-identifying data.
hosts:
  - {host: "203.0.113.10", port: 22, user: "root", name: "agentbox"}
