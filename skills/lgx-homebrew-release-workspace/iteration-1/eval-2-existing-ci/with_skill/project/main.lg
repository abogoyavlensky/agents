(ns gizmo.main
  (:require [gizmo.core :as core]
            [io :as io]
            [tiny-cli.core :as cli]))

(defn- version []
  (io/slurp (io/resource "VERSION")))

(def app
  {:name "gizmo"
   :version (version)
   :doc "Do the thing across all your repos."
   :commands [{:name "run"
               :doc "Run the thing."
               :run core/run}]})

(defn- main []
  (cli/run! app *command-line-args*))

; Top-level forms run during AOT compilation too. Guard so `lg -b` doesn't
; execute the CLI while bundling.
(when-not *compiling-aot*
  (main))
